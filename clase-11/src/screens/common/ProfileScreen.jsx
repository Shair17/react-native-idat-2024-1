import React from 'react';
import {View, Text} from 'react-native';

export const ProfileScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text style={{fontSize: 24, fontWeight: '600'}}>ProfileScreen</Text>
    </View>
  );
};
